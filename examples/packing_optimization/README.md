# Example: packing_optimization

Placeholder for a worked example in this category. Use `scripts/create_problem.py`
to scaffold a problem, then follow the loop in `CLAUDE.md` and `docs/01-research-loop.md`.
See the runnable examples in `examples/closed_form_constant` and
`examples/graph_construction`.
