# Problem Statement

> Template. Replace every placeholder with the concrete problem. Public file — no
> answer key, no hidden verifier output (`docs/11-evaluation-integrity.md`).

## Title
<short descriptive title>

## Area
<topic-pack area, e.g. extremal_graph_theory — link the relevant pack>

## Problem
<State the problem precisely. Define every object and parameter. Make explicit
what is being asked for: a number, a construction, an extremal object, a bound,
a closed form.>

## Parameters
- `<name>`: <meaning, range/value>
- ...

## Objective / what counts as a solution
<Exactly what the candidate must produce, and (if scored) how score is measured:
exact match, optimality, number of correct digits, objective value, etc.>

## Known facts / allowed assumptions
<Any givens, prior results the agent may assume, or restrictions on the regime.>

## Out of scope
<Anything explicitly not required.>

See `output_format.md` for the exact candidate format and `compliance_rules.md`
for final-answer restrictions.
